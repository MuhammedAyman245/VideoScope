import 'package:flutter/material.dart';

class bom extends StatelessWidget {
  static const String id = 'Bom';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        mainAxisAlignment: MainAxisAlignment.center,
        children: [],
      ),
    );
  }
}
